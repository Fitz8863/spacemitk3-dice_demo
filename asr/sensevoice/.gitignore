# 编译产物
build/
*.so
*.o
*.whl
__pycache__/
*.pyc
*.pyo

# Packages
*.egg
*.egg-info/
dist/

# 模型：小于 100MB 的文件随仓库（解码器/VAD/词表/配置）；主模型
# model_quant_optimized.onnx（227MB）超过 GitHub 单文件上限留在板端。
# 重建方式：下载 sensevoice.tar.gz 解压到 model/（见 README 2.2.1），
# 再删除 model_quant.onnx（未优化原始版，代码只加载
# model_quant_optimized.onnx）与 fig/、example/ 文档附件
model/model_quant_optimized.onnx
*.tar.gz

# Cache / 杂项
.cache/
wget.log
.mimosa/
