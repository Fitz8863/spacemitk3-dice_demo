# Board-only runtime payloads; provision these on the target board:
# sherpa-onnx shared libraries + headers extracted from the riscv64 wheel
# (include/ + lib/).  The matcha model directory (ONNX models, lexicon,
# rule FSTs, espeak-ng-data) ships with the repo -- every file is under
# the 100MB GitHub limit.
runtime/

# CMake build outputs
build/
build-*/

# Generated synthesis output
*.wav
