# Models

Place the board-compatible YOLOv8-seg ONNX model here, for example:

```text
models/yolov8n-seg.q.onnx
```

Model binaries are intentionally ignored by Git. Verify the actual input/output
metadata on the SpaceMIT K3 board before running the application.
