{
  "_note": "dice 专属的视觉 runtime 配置（模型与检测参数 + 硬件）。manifest 的 vision_profile.runtime_config 指向本文件，dice 只读这一份（runtime_config 是必填项）。整份替换、不做字段级合并：这里没写的键会回落到 C++ 编译期默认值，加字段请整份补齐。model 相对路径按本文件所在目录解析——'models/best.q.onnx' 即 backend/games/dice/models/best.q.onnx。",
  "model": "models/best.q.onnx",
  "conf": 0.45,
  "stable_frames": 30,
  "divider_detection": true,
  "camera": "/dev/video1",
  "device": "",
  "width": 1920,
  "height": 1080,
  "fps": 25,
  "intra_threads": 2,
  "ep_affinity": "14;15",
  "queue_depth": 2,
  "focus": -1,
  "zoom": 180,
  "video": {
    "webrtc_base_url": "http://127.0.0.1:8889"
  },
  "rtsp": {
    "enabled": true,
    "host": "127.0.0.1",
    "port": 8554
  }
}
